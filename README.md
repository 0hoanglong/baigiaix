# Web Tra Cứu Đề Thi & Giải Toán (Giaitoan)

Chào mừng đến với dự án **Web Tra Cứu Đề Thi**. Đây là ứng dụng web giúp học sinh các khối 10, 11, 12 dễ dàng xem trước đề thi và đáp án chi tiết dưới dạng PDF trực quan.

## 📖 Giới thiệu

Dự án được xây dựng với mục tiêu cung cấp công cụ tra cứu nhanh, nhẹ và dễ sử dụng. Hệ thống tích hợp trình xem PDF, chế độ ban đêm và cơ chế bảo mật nội dung đơn giản.

## ✨ Tính năng nổi bật

*   **Phân loại khoa học:** Dễ dàng lựa chọn theo Khối lớp (10, 11, 12) và Mã đề (A - H).
*   **Xem PDF trực tuyến:** Tích hợp `iframe` giúp xem đề thi ngay trên trình duyệt mà không cần tải về.
*   **Chế độ Sáng/Tối (Dark Mode):** Nút chuyển đổi giao diện linh hoạt (🌙/☀️) giúp bảo vệ mắt.
*   **Cổng bảo mật (Quiz Gate):** Hệ thống yêu cầu trả lời câu hỏi đố vui (ví dụ: "Con bò có biết bay không?") để truy cập nội dung, hạn chế spam.
*   **Giao diện Responsive:** Hiển thị tốt trên cả máy tính và thiết bị di động.

## 📂 Các phiên bản

Dự án bao gồm các giai đoạn phát triển:

### 🔹 Phiên bản V2 (`/v2`)
Phiên bản ổn định hiện tại với các chức năng cốt lõi:
*   Giao diện HTML/CSS thuần, dễ dàng chỉnh sửa.
*   Xử lý logic bằng JavaScript (Vanilla JS).
*   Tính năng chọn loại đề (17, 14) và bảng chọn đề thi trực quan.
*   Có sẵn code tích hợp widget trang trí (Sakana) nhưng đang tạm ẩn để tối ưu hiệu năng.

### 🔹 Phiên bản V3 (`/v3`)
Phiên bản nâng cấp với nhiều cải tiến (Đang phát triển/Cập nhật):
*   **Giao diện mới:** UI/UX được thiết kế lại hiện đại và mượt mà hơn.
*   **Tối ưu hóa:** Cấu trúc code gọn gàng, tăng tốc độ tải trang.
*   **Tính năng mở rộng:** Bổ sung thêm kho đề thi và các công cụ hỗ trợ học tập khác.

## 🛠️ Cài đặt & Sử dụng

Dự án là web tĩnh (Static Web), không yêu cầu cài đặt phức tạp:

1.  **Tải dự án:** Clone repository hoặc tải file `.zip` về máy.
2.  **Chạy cục bộ:**
    *   Vào thư mục `v2` hoặc `v3`.
    *   Mở file `index.html` bằng trình duyệt web bất kỳ (Chrome, Edge, Firefox...).
3.  **Triển khai (Deploy):**
    *   Upload toàn bộ thư mục lên hosting (Byethost, GitHub Pages, Vercel...).

## 🤝 Đóng góp

Mọi ý kiến đóng góp, báo lỗi hoặc yêu cầu tính năng mới xin vui lòng liên hệ tác giả hoặc tạo Issue trên kho lưu trữ.

---
*Dự án cá nhân - Chúc các bạn học tốt!*
